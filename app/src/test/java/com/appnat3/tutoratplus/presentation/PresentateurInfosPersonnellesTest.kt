package com.appnat3.tutoratplus.presentation
import com.appnat3.tutoratplus.presentation.Modele
import com.appnat3.tutoratplus.sourceDeDonnees.SourceDeDonnees
import kotlinx.coroutines.Dispatchers
import kotlinx.coroutines.newSingleThreadContext
import org.junit.After
import org.junit.Test
import java.util.Scanner
import java.io.PrintStream

import org.mockito.Mock
import org.mockito.Mockito
import org.mockito.junit.MockitoJUnitRunner
import org.junit.runner.RunWith
import org.junit.Assert.*
import org.junit.Before
@RunWith(MockitoJUnitRunner::class)
class PresentateurInfosPersonnellesTest {
    @Test
    fun `Étant donné on entre nos informations personnelles, alors on obtient les mêmes informations en retour`() {
        /*
        //Mise en place
        val modele: Modele = Modele()
        val source: SourceDeDonnees = Mockito.mock(SourceDeDonnees::class.java)
        val scan: Scanner = Mockito.mock(Scanner::class.java)

        val tuteur = Tuteur(1,"Mohamed Fatene", "programmation", arrayOf())
        //Prévoit l'entrée à saisir
        Mockito.`when`(scan.nextLine()).thenReturn("mohamed")
        Mockito.`when`(source.obtenirTuteursParCours("Programmation2")).thenReturn(listeTuteur)
        val modèle = Modele(source = source)
        */

    }
}